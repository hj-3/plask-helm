apiVersion: v1
kind: Service
metadata:
  name: plask-backend
  namespace: plask
  labels:
    app: plask-backend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: plask-backend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
